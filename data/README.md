# data

Versioned data the tooling reads -- not build inputs, not throwaway logs.

- `gpu_names_cache.json` -- GPU model list captured during the vast.ai
  benchmarking work. Kept as a reference snapshot; regenerate if a tool needs a
  fresher list.
